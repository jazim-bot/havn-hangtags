# Copy this into the Streamlit Cloud app's "Secrets" box (Settings → Secrets),
# or to .streamlit/secrets.toml for local testing. DO NOT commit the real file.
app_password = "choose-a-password-for-your-workers"
